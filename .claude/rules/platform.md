---
description: 平台隔离与 Windows 工具链规则入口
paths:
  - "src/platform/**"
  - "scripts/windows_*"
  - "scripts/package_windows_conpty.*"
---

本机薄适配：运行 `python3 scripts/resolve_agent_rules.py <paths...>` 并阅读输出；规则正文只在 `docs/AGENT_RULES/platform.md`。
