---
description: agent 检测与 manifest 规则入口
paths:
  - "src/detect/**"
  - "distribution/agent-detection/**"
---

本机薄适配：运行 `python3 scripts/resolve_agent_rules.py <paths...>` 并阅读输出；规则正文只在 `docs/AGENT_RULES/detection.md`。manifest 改动前必须采集真实 pane 证据。
